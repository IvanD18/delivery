package application.mapper;

import application.dto.ProductResponseDto;
import application.dto.SupplyResponseDto;
import application.entity.HistoryEntity;
import application.entity.ProductEntity;

import java.util.stream.Collectors;

public class Mapper {
//    public static HistoryEntity historyDtoToEntity(SupplyResponseDto supplyResponseDto){
//        HistoryEntity historyEntity = new HistoryEntity();
//        historyEntity.setProducts(supplyResponseDto.getProducts().stream().map(Mapper::productDtoToEntity).collect(Collectors.toList()));
//    }
//
//    public static ProductEntity productDtoToEntity(ProductResponseDto productResponseDto){
//        ProductEntity productEntity = new ProductEntity();
//        productEntity.setPrice(productResponseDto.getPrice());
//        productEntity.setStartTimestamp(productResponseDto.getStart());
//        productEntity.setEndTimestamp(productResponseDto.getEnd());
//        productEntity.set
//    }
}
